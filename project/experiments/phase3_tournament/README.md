# Phase 3 Placeholder

Tournament-phase experiment artifacts are not active here yet.

When activated, keep run outputs under `project/experiments/` with an index entry in `project/experiments/reports/EXPERIMENT_INDEX.md`.
